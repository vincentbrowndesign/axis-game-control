# ---------------------------------------------------------------------------
# Axis Tracking V1 — Python dependencies
# ---------------------------------------------------------------------------

# Core detection — RF-DETR Nano (Roboflow)
# https://github.com/roboflow/rf-detr
rfdetr>=0.1.0

# Detection normalization + ByteTrack
# supervision ships ByteTracker natively (sv.ByteTracker)
# This covers the "trackers" requirement — no separate package needed
supervision>=0.22.0

# Linear assignment solver required by ByteTrack internals
lapx>=0.5.2

# Frame capture + decoding
opencv-python>=4.9.0
numpy>=1.26.0
pillow>=10.0.0

# ---------------------------------------------------------------------------
# Optional — WebSocket server (server.py)
# ---------------------------------------------------------------------------
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0
python-multipart>=0.0.9
