import { ClipRoomNew } from "../../components/clip-room/ClipRoomNew";

export const metadata = { title: "Record Clip — Axis" };

export default function RecordPage() {
  return <ClipRoomNew mode="record" />;
}
