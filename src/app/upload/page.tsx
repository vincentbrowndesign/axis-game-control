import { ClipRoomNew } from "../../components/clip-room/ClipRoomNew";

export const metadata = { title: "Upload Clip — Axis" };

export default function UploadPage() {
  return <ClipRoomNew mode="upload" />;
}
